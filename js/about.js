/* ============================================================
   about.js — behavior unique to about.html
   SAKSHI object (from data.js) is already loaded and available here.
   ============================================================ */

document.addEventListener("DOMContentLoaded", () => {
  // TODO: render about content from the SAKSHI data object
});
